# Test profile: use authenticated MongoDB URI (matches compose.yaml credentials)
spring.data.mongodb.uri=mongodb://admin:changeit@localhost:27017/mydatabase?authSource=admin

# Disable docker-compose auto-configuration for tests (we manage our own container)
spring.docker.compose.enabled=false

